A minimal Angular app that connects to a Hugging Face Falcon-7B model for chat. 

Drop your HF_API_TOKEN into a root .env, run npm install then npm run start:secure to generate environment.ts and launch at http://localhost:4200. 

Swap the endpoint in AiChatService if you need a different HF model.